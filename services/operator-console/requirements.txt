# Runtime dependencies for the read-only operator console.
fastapi>=0.115,<0.117
uvicorn[standard]>=0.32,<0.36

# Tails ontology.mutations and ontology.commands. The snappy extra is not
# optional: the Go resolution engine publishes ontology.mutations with Snappy
# compression, and without the codec every fetch fails to decompress.
aiokafka[snappy,lz4,zstd]>=0.12,<0.13

# Reads live twin state written by the engine. Read-only — this service never
# writes a key.
redis>=5.1,<7
